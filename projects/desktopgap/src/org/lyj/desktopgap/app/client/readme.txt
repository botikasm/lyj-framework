Client package is built using browserify.
To install browserify and exorcist use this commands:

npm install browserify -g
npm install exorcist -g

For further informations about browserify follow this link:
https://github.com/substack/browserify-handbook