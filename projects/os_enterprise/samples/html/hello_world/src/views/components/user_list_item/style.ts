export default function style(uid: string, props?: any): string {

    // main
    const main: string = `
        <style>
            
        </style>      
    `;

    return `
        ${main}
           
    `;
};