/**
 * GLOBAL VARIABLES
 */

const root: any = window;

const globals = {

    // expone window
    root: root,
    Materialize: root['Materialize'], // materialize
    $: root['$'], // jQuery


};
export default globals;